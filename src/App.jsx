import React, { Component } from 'react';
const test = `${true ? 'but' : 'let'}`;
class App extends Component{
  render() {
    return (
      <div>
        <h1>Hello Christian!</h1>
      </div>
    );
  }
}

export default App;